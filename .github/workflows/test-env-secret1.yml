on: 
  workflow_dispatch

jobs:
  echo-test-secret1:
    runs-on: ubuntu-latest
    env:
      name: int
    steps:
      - name: "echo TEST from ${{ env.NODE_ENV }}"
        run: |
          echo ${{ secrets.TEST }} | sed 's/./& /g'
